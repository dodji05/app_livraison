{% block collection_widget %}

        {% if prototype is defined %}
            {% set attr = attr|merge({'data-prototype': block('collection_item_widget') }) %}
        {% endif %}
        <div class="collection" {{ block('widget_container_attributes') }}>
            {{ form_errors(form) }}
            <table class="table table-bordered table-hover">
                <thead>
                <tr>
                    <th class="text-center">
                         Nom du plats
                        <i class="text-danger">*</i>
                    </th>
                    <th class="text-center" >
                        <span> Prix d'achat </span>
                    </th>
                    <th class="text-center">
                        <span contenteditable> Prix de vente</span>
                    </th>
                    


                </tr>
                </thead>
				
                <tbody>
     {% set fieldNum = 1 %}
     <tr>
                {% for rows in form %}

                    {% for row in rows %}
                       {#{%  if fieldNum == 1 %}#}
                           {#{% set class = "ligne_vente_code" %}#}
                           {#<td>  <span contenteditable>  {{ form_widget(row) }}</span></td>#}
                       {#{% endif %}#}
                       {#{%  if fieldNum == 2 %}#}
                           {#{% set class = "vente_id" %}#}
                           {#<td>   <span contenteditable>  {{ form_widget(row) }}</td>#}
                       {#{% endif %}#}
                       {#{%  if fieldNum == 3 %}#}
                           {#{% set class = "vente_qte" %}#}
                           {#<td>    {{ form_widget(row) }}</td>#}
                       {#{% endif %}#}

                       {#{%  if fieldNum == 4 %}#}
                           {#{% set class = "vente_pV" %}#}
                           {#<td>    {{ form_widget(row) }}</td>#}
                       {#{% endif %}#}

                       {#{%  if fieldNum == 5 %}#}
                           {#{% set class = "vente_bV" %}#}
                           {#<td>    {{ form_widget(row) }}</td>#}
                       {#{% endif %}#}

                       {#{%  if fieldNum == 6 %}#}
                           {#{% set class = "vente_total" %}#}
                           {#<td>    {{ form_widget(row) }}</td>#}
                       {#{% endif %}#}
                       {#{{ form_widget(row, { 'attr': { 'class': class }}) }}#}
                        <td>
						
                          {{ form_widget(row) }}
                        </td>
						
						

                    {% set fieldNum = fieldNum + 1 %}
                    {% endfor %}

     </tr>
	 
                {% endfor %}

               {{ form_rest(form) }} 
                </tbody>
                <tfoot>


                
                </tfoot>
            </table>



        </div>

     <div class="row">
         <div class="col-lg-12">
		    <a id="addItem" class="label label-default" aria-hidden="true">Ajouter un autre plat</a>
         </div>
     </div>
   
{% endblock collection_widget %}

{% block collection_item_widget %}
    
        <tr>
            {% for row in prototype %}
                <td>
                   {{ form_widget(row) }}
                </td>
            {% endfor %}
			
        </tr>
   
{% endblock collection_item_widget %}